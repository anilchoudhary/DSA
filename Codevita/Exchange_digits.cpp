#include<bits/stdc++.h>
using namespace std;


int main()
{
    long long int a,b;
    cin>>a>>b;
    vector<int> arr,brr;


    while(a>0)
    {
        arr[i] = a%10;
        a = a/10;
    }
    while(b>0)
    {
        brr[i] = b%10;
        b = b/10;
    }
    int an = arr.size();
    int bn = brr.size();




}
